import React from "react";
import s from "./Footer.module.scss";

const Footer = () => {
  return (
    <>
      <footer className={s.footerWrapper}>Footer</footer>
    </>
  );
};

export default Footer;
